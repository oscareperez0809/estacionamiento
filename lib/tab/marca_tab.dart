import 'package:flutter/material.dart';

class MarcaTab extends StatelessWidget {
  const MarcaTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'MarcaTab',
        style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold),
      ),
    );
  }
}
